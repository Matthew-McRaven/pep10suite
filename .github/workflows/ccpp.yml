name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Install libmesa
      run:  apt install libmesa-dev
    - name: Install Qt
      uses: jurplel/install-qt-action@v1
    - uses: actions/checkout@v1
    - name: list
      run: ls -a
    - name: qmake
      run: qmake pep10unit/pep10unit.pro
    - name: make qmake_all
      run: make qmake_all
    - name: make
      run: make
    - name: run unit
      run: ls -a
